stages:
  - build
  - deploy

build:
  stage: build
  script:
    - echo "Building Docker images"
    - docker-compose build

deploy:
  stage: deploy
  script:
    - echo "Deploying Docker Compose app"
    - docker-compose up -d